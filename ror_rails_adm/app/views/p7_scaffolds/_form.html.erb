<%= form_for(p7_scaffold) do |f| %>
  <% if p7_scaffold.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(p7_scaffold.errors.count, "error") %> prohibited this p7_scaffold from being saved:</h2>

      <ul>
      <% p7_scaffold.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :birthday %>
    <%= f.date_select :birthday %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
